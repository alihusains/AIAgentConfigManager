{
  "name": "agentcontrol",
  "version": "0.1.0",
  "description": "AgentControl — one registry, every agent, in sync. Manage models, providers, MCP servers, and permissions across AI coding agents.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "agm": "./dist/bundle-entry.js"
  },
  "files": ["dist", "vendor"],
  "scripts": {
    "build": "tsc",
    "prepack": "node scripts/prepare-pack.mjs",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "biome lint src"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "inquirer": "^9.2.0",
    "table": "^6.8.0"
  },
  "devDependencies": {
    "@ai-agent-config/core": "workspace:*",
    "@types/inquirer": "^9.0.0",
    "@types/node": "^22.0.0",
    "@types/table": "^6.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
